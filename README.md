## 📋 О проекте

Данный проект представляет собой микросервисную инфраструктуру для отслеживания расписани и изменений в расписании образовательного процесса, включающую:

- **🔐 Auth Service** - сервис аутентификации и управления пользователями (Go)
- **🤖 Bot Service** - телеграм бот для взаимодействия с пользователями (Python)
- **🗄️ PostgreSQL** - база данных
- **🔗 n8n** - платформа для автоматизации рабочих процессов
- **📚 Swagger UI** - документация API

## ⚙️ Предварительные требования

### Вариант 1: Docker Desktop

1. **Установите Docker Desktop для Windows:**
   - Скачайте с [официального сайта](https://www.docker.com/products/docker-desktop/)

2. **Проверьте установку:**
   ```cmd
   docker --version
   docker-compose --version
   ```

### Вариант 2: Виртуальная машина с Linux

Если Docker Desktop не работает, используйте один из вариантов:

1. **VirtualBox + Ubuntu:**
   - Установите [VirtualBox](https://www.virtualbox.org/)
   - Скачайте [Ubuntu Server](https://ubuntu.com/download/server)
   - Создайте виртуальную машину (рекомендуется: 2+ ядра, 4+ GB RAM, 20+ GB HDD)
   - Установите Docker в виртуальной машине:
     ```bash
     curl -fsSL https://get.docker.com -o get-docker.sh
     sudo sh get-docker.sh
     sudo usermod -aG docker $USER
     ```

2. **WSL2 + Docker (альтернативный вариант):**
   - Включите WSL2 в Windows Features
   - Установите Ubuntu из Microsoft Store
   - Установите Docker в WSL2

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```cmd
# Через командную строку
git clone <адрес-вашего-репозитория>
cd <папка-проекта>

# Или скачайте ZIP архив и распакуйте
```

### 2. Настройка окружения

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# База данных
POSTGRES_DB=auth_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Сервисы
DEBUG=true
```

**🔑 Как получить Telegram Bot Token:**
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Используйте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Вставьте токен в файл `.env` вместо `your_telegram_bot_token_here`

### 3. Запуск инфраструктуры

```cmd
# Запуск всех сервисов
docker-compose up -d

# Или с выводом логов
docker-compose up
```

### 4. Проверка работоспособности

После запуска проверьте статус сервисов:

```cmd
docker-compose ps
```

Должны быть активны все сервисы:
- ✅ auth-service
- ✅ bot-service  
- ✅ postgres
- ✅ n8n
- ✅ swagger-ui

## 🌐 Доступ к сервисам

После успешного запуска сервисы будут доступны по следующим адресам:

| Сервис | URL | Порт | Описание |
|--------|-----|------|----------|
| **n8n** | http://localhost:5678 | 5678 | Платформа автоматизации |
| **Swagger UI** | http://localhost:8081 | 8081 | Документация API |
| **Auth Service API** | ❌ Внутренняя сеть | - | Доступен только внутри Docker |
| **Bot Service API** | ❌ Внутренняя сеть | - | Доступен только внутри Docker |

## 🧪 Тестирование работы

### 1. Проверка Auth Service через Swagger

1. Откройте http://localhost:8081 в браузере
2. Вы должны увидеть документацию API для auth-service

### 2. Проверка Telegram бота

1. Найдите вашего бота в Telegram по имени
2. Отправьте команду `/start`
3. Бот должен ответить и зарегистрировать вас в системе
4. Отправьте любое сообщение - бот ответит эхом

### 3. Проверка n8n

1. Откройте http://localhost:5678
2. Создайте простой workflow для тестирования

## 🛠️ Полезные команды

### Управление сервисами

```cmd
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов конкретного сервиса
docker-compose logs auth-service
docker-compose logs bot-service

# Перезапуск конкретного сервиса
docker-compose restart bot-service

# Просмотр статуса всех сервисов
docker-compose ps
```

### Работа с Docker

```cmd
# Просмотр всех контейнеров
docker ps -a

# Просмотр образов
docker images

# Очистка системы
docker system prune

# Просмотр логов контейнера
docker logs <container_name>
```

## 🔧 Возможные проблемы и решения

### ❌ Проблема: "Port is already allocated"
**Решение:** Освободите занятый порт или измените порт в `docker-compose.yml`

### ❌ Проблема: "Cannot connect to the Docker daemon"
**Решение:**
- Убедитесь, что Docker Desktop запущен
- Попробуйте запустить командную строку от имени администратора

### ❌ Проблема: Сервисы не запускаются
**Решение:**
```cmd
# Просмотр детальных логов
docker-compose logs

# Пересборка образов
docker-compose build --no-cache
docker-compose up -d
```

### ❌ Проблема: Бот не отвечает
**Решение:**
- Проверьте TELEGRAM_BOT_TOKEN в файле .env
- Убедитесь, что бот запущен: `docker-compose logs bot-service`
- Перезапустите бота: `docker-compose restart bot-service`

### ❌ Проблема: Нет доступа к базе данных
**Решение:**
- Проверьте, что PostgreSQL контейнер запущен
- Убедитесь в правильности переменных окружения
- Проверьте логи: `docker-compose logs postgres`

## 📁 Структура проекта

```
schedule-bot/
├── services/
│       auth-service/          # Сервис аутентификации (Go)
│       bot-service/           # Телеграм бот (Python)
├── docker-compose.yml     # Конфигурация всех сервисов
├── .env                   # Переменные окружения
└── README.md             # Эта документация
```

## 🆘 Получение помощи

Если у вас возникли проблемы:

1. **Проверьте эту документацию** - большинство проблем уже описаны
2. **Посмотрите логи** - используйте `docker-compose logs <service-name>`
3. **Перезапустите сервисы** - `docker-compose down && docker-compose up -d`
4. **Обратитесь к преподавателю** - предоставьте информацию о проблеме и логи

## 📝 Дополнительные настройки

### Добавление своих сервисов

Вы можете расширить инфраструктуру, добавив свои сервисы в `docker-compose.yml`.
